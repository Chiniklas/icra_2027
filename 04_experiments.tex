% !TeX root = main.tex
\subsection{Evaluation Metrics}

We evaluate performance using two complementary metrics.

\paragraph{Maximum Cycle Count}
One cycle comprises a target-angle turn followed by successful release, return,
and re-engagement. Maximum cycle count is the number of consecutive complete
cycles before the first grasp loss, transition failure, safety termination, or
timeout; an incomplete final cycle is not counted.

\paragraph{Threading Tracking Error}
Threading tracking error measures how closely the nut's axial motion follows
the thread pitch during turning. Over turning samples $\mathcal{T}$, we report
the axial coupling RMSE
\begin{equation}
  E_{\mathrm{thr}}=\sqrt{\frac{1}{|\mathcal{T}|}\sum_{t\in\mathcal{T}}
  \left[(d_t-d_{b_k})+\frac{\rho}{2\pi}(\theta_t-\theta_{b_k})\right]^2},
  \label{eq:threading_tracking_error}
\end{equation}
where $b_k$ is the start of the cycle containing sample $t$. Lower error
indicates closer agreement with the desired helix.

\subsection{Experimental Setup}

We simulate a seven-DoF Franka Research~3 (FR3) arm equipped with an Inspire
Hand RH56DFX using Isaac Sim~5.1.0 and Isaac Lab~2.2.1. The 9-D action space
encodes a six-DoF OSC target and three index--thumb joint commands. At
\SI{15}{Hz},
the student converts a calibrated $320\times180$ depth image into 4096 cropped
points and fuses a 64-dimensional DP3 feature with proprioception, progress,
and phase; physics runs at \SI{120}{Hz}. For the retained M24 run, we train the
PPO teacher for 200 epochs in 512 environments and collect 20 successful
ten-cycle demonstrations (19,486 frames) from 32 environments, with 30 random
arm impulses per demonstration. We train the flow-matching student for 300
epochs using eight-step action chunks. All stages use one GPU.

% TODO: Validate and report the GPU model, wall-clock training times,
% domain-randomization ranges, initialization distributions, and the final
% multi-object training/data budgets used for the paper.

\begin{figure}[t]
  \centering
  %\includegraphics[width=\columnwidth]{figures/simulation_scene_placeholder.png}
  \includegraphics[width=\columnwidth]{figures/policy_visuals/teacher/threading_v2_teacher_120_envs.png}
  %\caption{Simulated robot, workspace, and threading task.}
  \caption{Teacher policy in simulation.}
  \label{fig:simulation_scene}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/policy_visuals/student/no_robot_randomization/view_01.png}
  \caption{Student policy in simulation. The randomized environment and object
  textures increase visual diversity during training to improve robustness of
  the vision-based policy.}
  \label{fig:student_texture_randomization}
\end{figure}

The physical platform comprises the same FR3 arm and Inspire Hand RH56DFX. An
Intel RealSense D415 supplies the student's depth observations through the same
OSC interface used in simulation. Teacher and student training uses only
hexagonal nuts. We test M24, M30, and M36 hexagonal nuts in distribution and
hold out the knurled (round), octagonal, and square variants of each size for
out-of-distribution evaluation.

% TODO: Validate and report the control and policy frequencies, camera
% calibration/resolution/frame rate, workspace and safety limits, trial counts,
% initialization distributions, and success/failure criteria.

\begin{figure}[t]
  \centering
  %\includegraphics[width=\columnwidth]{figures/hardware_family_placeholder.png}
  \includegraphics[width=\columnwidth]{figures/setup-nut-bolt-family/NutBoltHolderFamilyOnly3.jpeg}
  \caption{Nut--bolt families used for real-world evaluation. From left to
  right, the M24$\times$3, M30$\times$3.5, and M36$\times$4 families are shown
  with their bolts mounted in holders. Each family comprises an ISO metric
  hexagonal nut and three held-out shape variants: knurled (round), octagonal,
  and square.}
  \label{fig:hardware_family}
\end{figure}

\subsection{Ablations}

We compare four policy-learning variants while keeping the architecture,
dataset budget, evaluation protocol, and all unrelated settings fixed.

\paragraph{Behavior Cloning}
Train the student policy using supervised behavior cloning on the initially
collected teacher demonstrations only.

\paragraph{Online DAgger}
Iteratively execute the current student policy, query the teacher online for
corrective actions on visited states, aggregate the new samples, and retrain the
student policy.

\paragraph{Offline DAgger}
Train on a fixed, pre-collected dataset that includes teacher corrections for
states visited by an intermediate student policy, without additional online
interaction during final training.

\paragraph{Ours}
Train once on the fixed dataset of hybrid-expert demonstrations and offline
human corrections using the proposed flow-matching imitation objective; no
student rollouts or online relabeling are used during training.

\subsection{Quantitative Results}

\input{table/maximum_cycle_count}

\input{table/thread_tracking_error}

\input{table/success_rate}

Interpret the three comparisons instead of repeating table entries. Discuss
maximum cycle count, threading tracking error, and success rate together, and tie
every central claim to the corresponding table or statistical test.

\subsection{Qualitative and Real-World Results}

Use vector figures (PDF) when possible, and ensure plot text remains legible at
the final column width. Discuss failure cases rather than reporting only the
best outcomes in the quantitative tables.
