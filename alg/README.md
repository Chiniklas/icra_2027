# Algorithms

Keep reusable algorithm environments in separate `.tex` files here and include
them from the relevant numbered section file. `algorithm_template.tex` is a
compiling placeholder based on the reference paper's pseudocode placement.
