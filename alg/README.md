# Algorithms

Keep reusable algorithm environments in separate `.tex` files here and include
them from the relevant numbered section file. `algo_1.tex` contains the offline
flow-distillation procedure used by the paper.
