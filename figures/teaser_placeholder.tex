% Full-width page-one teaser placeholder. Replace this TikZ picture with
% \includegraphics[width=\linewidth]{figures/your_teaser.pdf} when ready.
\begin{tikzpicture}[
  font=\small,
  panel/.style={
    draw=black!55,
    rounded corners=2pt,
    minimum height=1.12in,
    text width=0.265\textwidth,
    align=center,
    inner sep=7pt
  },
  flow/.style={-{Latex[length=2.2mm]}, line width=0.8pt, draw=black!70}
]
  \node[panel, fill=blue!7] (problem) {
    \textbf{1. Problem setting}\\[3pt]
    Replace with a task image or schematic.\\[3pt]
    \footnotesize Show the input, robot, environment, and key challenge.
  };
  \node[panel, fill=orange!9, right=0.55in of problem] (method) {
    \textbf{2. Core idea}\\[3pt]
    Replace with the proposed method.\\[3pt]
    \footnotesize Highlight only the modules needed to explain the novelty.
  };
  \node[panel, fill=green!9, right=0.55in of method] (result) {
    \textbf{3. Headline result}\\[3pt]
    Replace with a result image or plot.\\[3pt]
    \footnotesize State the main measurable improvement and comparison.
  };

  \draw[flow] (problem) -- (method);
  \draw[flow] (method) -- (result);
\end{tikzpicture}
